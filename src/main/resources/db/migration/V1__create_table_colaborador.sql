CREATE TABLE colaborador(
   id BIGNT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
   nome VARCHAR(255) NOT NULL,
   cargo VARCHAR(255) NOT NULL,
   matricula VARCHAR(255) NOT NULL,
   data_criado TIMESTAMP NOT NULL
)